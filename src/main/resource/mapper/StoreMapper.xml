<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.store">
    <select id="findAll" resultType="Store">
        SELECT id,product_id,member_id,store_date,store_count,status,store_price
        FROM store
    </select>

<!--    <select id="findByStoreId" parameterType="long" resultType="Store">
        SELECT id,product_id,member_id,store_date,store_count,status,store_price
        FROM store
        WHERE id = #{id}
    </select>
-->
   <insert id="insert">
       INSERT INTO store (product_id, member_id, store_date, store_count, status,store_price)
       VALUES (#{product_id},#{member_id},#{store_date},#{store_count},#{status},#{store_price})
   </insert>

    <update id="update" parameterType="Store">
        UPDATE store
        SET
        id = #{id}, store_date = #{store_date}, 
        store_count=#{store_count}, status = #{status}, store_price = #{store_price}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Store">
        DELETE FROM store
        WHERE id = #{id}
    </delete>
</mapper>